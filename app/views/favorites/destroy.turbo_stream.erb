<%= turbo_stream.replace dom_id(@game, :favorite_toggle), partial: 'favorites/toggle_button' %>

<% if defined?(@favorites) %>
  <%= turbo_stream.replace 'favorites_list', partial: 'favorites/list' %>
<% end %>
