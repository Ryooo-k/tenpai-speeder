<div class="absolute <%= build_river_position_class(player) %>">
  <div class="grid grid-cols-6 grid-rows-4 place-items-center">
    <% player.rivers.each do |river| %>
      <div class="relative">
        <% if river.riichi? %>
          <%= image_tag("tiles/rotated_#{river.suit}_#{river.number}.gif", class: "w-[3.8cqw]") %>
        <% else %>
          <%= image_tag("tiles/#{river.suit}_#{river.number}.gif", class: "w-[3.8cqw]") %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
