<% player = @game.user_player %>

<section class="relative h-[calc(100dvh-200px)] overflow-y-auto pr-3 tips-scroll" aria-labelledby="tips-heading">
  <div class="relative flex flex-col gap-2">
    <h2 id="tips-heading" class="text-lg font-semibold text-cyan-200 tracking-[0.15em]">TIPS</h2>
    <p class="text-xs text-slate-300/80">
      現在の手牌状況、面子手のヒントを表示します。
    </p>
  </div>
  <div class="relative mt-4 space-y-4">
    <% if player.waiting_wining_tile? %>
      <%= render partial: "games/tips/tenpai_summary" %>
    <% else %>
      <%= render partial: "games/tips/hand_efficiency" %>
    <% end %>
  </div>
</section>
