<div class="min-h-screen w-full bg-slate-950 text-slate-100">
  <div class="mx-auto w-full space-y-8 px-4 pb-6 pt-6 sm:px-6 lg:px-8">
    <%= render "header" %>
    <div class="grid gap-6 lg:grid-cols-[minmax(0,1.75fr)_minmax(280px,1fr)] 2xl:grid-cols-[minmax(0,2fr)_minmax(320px,1fr)]">
      <%= render "mahjong_table" %>
      <aside class="space-y-6">
        <%= render "mahjong_table_overview" %>
        <%= render "tips" %>
      </aside>
    </div>
  </div>
</div>
