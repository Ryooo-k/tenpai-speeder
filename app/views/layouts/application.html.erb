<!DOCTYPE html>
<html>
  <head>
    <% og_title = content_for?(:og_title) ? yield(:og_title) : (content_for?(:title) ? yield(:title) : "Tenpai Speeder") %>
    <% og_description = content_for?(:og_description) ? yield(:og_description) : (content_for?(:meta_description) ? yield(:meta_description) : 'Tenpai Speederは、麻雀中級者以下向けの打牌・牌効率トレーニングサービスです。ブラウザだけでリーチ判断や終盤判断をテンポ良く練習できます。') %>
    <% og_image = content_for?(:og_image) ? yield(:og_image) : "#{request.base_url}/logo.png?v=2" %>
    <% og_url = content_for?(:og_url) ? yield(:og_url) : request.original_url %>
    <title><%= og_title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="<%= content_for?(:meta_description) ? yield(:meta_description) : og_description %>">
    <meta name="keywords" content="<%= content_for?(:meta_keywords) ? yield(:meta_keywords) : '麻雀,牌効率,リーチ,テンパイ,シミュレーター,練習,テンパイスピーダー' %>">
    <meta property="og:title" content="<%= og_title %>">
    <meta property="og:description" content="<%= og_description %>">
    <meta property="og:image" content="<%= og_image %>">
    <meta property="og:url" content="<%= og_url %>">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="Tenpai Speeder">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= og_title %>">
    <meta name="twitter:description" content="<%= og_description %>">
    <meta name="twitter:image" content="<%= og_image %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/logo.png" type="image/png">
    <link rel="icon" href="/logo.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/logo.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if current_path != root_path %>
      <%= render "application/header", logo_link_path: (content_for?(:logo_link_path) ? yield(:logo_link_path) : home_path) %>
    <% end %>
    <div id="flash">
      <%= render 'application/flash' %>
    </div>
    <main class="flex-1">
      <%= yield %>
    </main>
    <%= render "application/footer" %>
  </body>
</html>
